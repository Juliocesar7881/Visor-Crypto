# Copy this file to android/keystore.properties for local release builds.
# Never commit the real keystore file or real passwords.
# If a keystore was ever committed to Git history, rotate the upload key in Play Console.
storeFile=upload-key-playstore.jks
storePassword=CHANGE_ME
keyAlias=visor-upload
keyPassword=CHANGE_ME
